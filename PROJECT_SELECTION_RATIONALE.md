# 🎯 프로젝트 선정 이유 (Why This Project)
> **팀 프로젝트**: 4인 실시간 AR 섀도우 복싱 & 배틀 아레나
> **참조 문서**: `ref/DS-vision-project_ot.pptx`(과제 OT), `ref/th_index.html`(실습6 · 비전 복싱), `ref/ds-vision-board-final-updated.html`(8개 분야 CV 프로젝트 보드)
> **작성일**: 2026-08-27

---

## 1. 출발점 — 과제가 요구하는 것, 그리고 우리가 가진 자원

과제 OT(`ref/DS-vision-project_ot.pptx`)는 평가 기준을 네 가지로 명시합니다.

| 평가 기준 | 원문 요지 |
|---|---|
| **Practicality (실용성)** | 사람들이 실제로 쓸 만한 도구인가 |
| **Not trivial (비단순성)** | 아무나 만들 수 있는 게 아니라, 우리의 실력이 드러나는가 |
| **Creative idea (창의성)** | 전에 본 적 없는 것인가 |
| **Problem solving (문제 해결)** | 실제로 부딪힌 문제를 어떻게 풀었고, **수치로 얼마나 개선했는가 (Show Numbers)** |

동시에 OT는 위험 신호도 짚어줍니다 — *"지난 기수 최고 프로젝트는 웹캠으로 하는 게임이었다. 흔한 소재는 댄스, 골프 스윙, 운동 PT. 문제는 정확도가 생각보다 어렵고, 결과물이 서로 비슷해 보인다는 것 — 그래서 시각효과가 차별점이 된다."*

즉, **"웹캠 + 포즈 인식 게임" 자체는 이미 예상된 카테고리**이고, 여기서 좋은 점수를 받으려면 (a) 남들과 다른 축의 창의성, (b) 실제로 겪은 CV 문제와 정량적 개선 스토리, 두 가지가 반드시 있어야 한다는 뜻입니다.

여기에 우리가 가진 자원이 하나 더 있었습니다: **개인 노트북 각 1대씩, 도합 4대의 PC**. "혼자 웹캠 앞에서 손짓하는" 흔한 1인용 데모가 아니라, **4대를 동시에 엮어서 진짜 다자간 인터랙션을 만들면 참신하다**는 것이 프로젝트의 첫 아이디어였습니다.

---

## 2. 왜 "복싱"인가 — 주어진 실습에서 출발해 의도적으로 벗어나기

교수님이 실습 자료로 제공한 `ref/th_index.html` ("실습6 · 비전 복싱")은 이미 이 소재의 CV적 뼈대를 보여줍니다.

> MediaPipe Pose Landmarker로 양팔·머리 관절을 추적해 잽·훅·어퍼컷과 더킹·위빙을 규칙 기반으로 판정 → **2인**이 WebRTC P2P로 직접 연결, 판정은 각자 로컬 계산 후 결과만 동기화.

이 실습이 우리에게 준 것은 두 가지입니다.

1. **소재의 타당성**: "포즈 기반 복싱"이 이미 수업 차원에서 CV 태스크로 검증된 소재라는 확신.
2. **정확히 피해야 할 함정**: 실습6을 그대로 재현하면 "아무나 만들 수 있는" 데모(Not trivial 기준 미달)에 머무릅니다. 2인·규칙기반·1개 화면이라는 실습6의 틀을 **그대로 베끼지 않고, 어디를 넘어설지**가 우리 프로젝트 설계의 핵심 결정이었습니다.

우리가 실습6 대비 넘어선 축은 셋입니다.

| 축 | 실습6 (레퍼런스) | 우리 프로젝트 |
|---|---|---|
| **플레이어 수 / 인터랙션 구조** | 2인, P2P 1:1 | **4인, 4대 PC가 하나의 서버(FastAPI+WebSocket)에 실시간 동기화** → 진짜 다자간 아레나 |
| **판정 방식** | 정적 규칙(임계값) | 규칙 기반 + **GPU 시계열 딥러닝(BiLSTM)** 병행, 정량 비교 |
| **화면 구성** | 각자 화면에 상대 캠 썸네일 | 4인 통합 **3D 관제 뷰(Host arena)** + 개인 1인칭 대전 뷰 분리 |

즉 "복싱"이라는 소재 선택 자체보다, **"주어진 2인용 실습을 4대 PC 기반의 실시간 다자간 시스템으로 일반화한 것"**이 창의성·비단순성 기준에 대한 우리의 답입니다. 이 확장은 단순 기능 추가가 아니라, 4명의 포즈 스트림을 동시에 받아 하나의 좌표계(링) 안에서 충돌·데미지·생존을 실시간으로 합의해야 하는 **분산 실시간 인터랙션 문제**를 새로 만들어냅니다 — 이것이 "PC 4대 활용 = 참신함"이라는 최초 아이디어가 CV/시스템 문제로 구체화된 지점입니다.

---

## 3. CV 기술 관점에서 본 선정 이유

레퍼런스 보드(`ref/ds-vision-board-final-updated.html`)는 CV를 8개 분야로 나눕니다. 복싱이라는 소재는 이 중 특히 두 분야와 자연스럽게 맞물리고, 각 분야 안에서 우리가 실제로 부딪힌 문제 → 해결 → 수치 개선의 스토리가 있습니다 (`iter4/CV_TECHNIQUES.md` 상세 매핑 참고).

### (1) `04. 사람 자세 · 3D 모션` — 이 프로젝트의 핵심 분야
보드가 소개하는 `MediaPipe Pose Landmarker` 계열을 그대로 쓰되, 33개 전신 랜드마크 중 **판정에 실제로 기여하는 상체 7개(코·양어깨·양팔꿈치·양손목)만 선별**했습니다.

- **왜 Hands(42노드)에서 Pose(7노드)로 바꿨나**: 기존엔 손가락 21개×2를 추적하면서도 실제로는 손목 2개만 쓰고, 이동은 "양손 손목의 화면 위치"라는 대용 신호로 근사 → 손만 움직여도 캐릭터가 움직이는 오류. Pose 7노드로 바꾸면서 **추적 비용은 42→7로 줄이고, 인식 대상은 손가락→상체 전체(기울임, 스텝, 가드)로 확장**했습니다.
- **왜 복싱이 이 분야를 잘 보여주는 소재인가**: 잽/훅/어퍼컷/가드처럼 **명확히 구분되는 동작 클래스**가 존재해서, 포즈 분류 문제를 깔끔한 지도학습 문제로 환원할 수 있습니다. 댄스나 운동 PT처럼 "동작이 맞았는지 애매하게 판정"하는 대신, 복싱은 판정 기준(팔 뻗음 속도·각도·궤적)이 명시적이라 **Show Numbers가 가능한 소재**입니다.
- **정량 개선**: 정적 룰베이스 정확도 33.3% → 시계열 **BiLSTM 100%** (합성 데이터 기준, `motion_learning/eval_results.json`).

### (2) `05. 깊이 · 카메라 · 3D` — 별도 모델 없이 얻은 3D의 활용
보드의 이 분야는 Depth Anything 3, VGGT-Ω처럼 **별도 depth 모델**을 쓰는 카드들입니다. 우리는 별도 모델을 추가하는 대신, 이미 쓰고 있는 Pose 추정기가 부산물로 내주는 `poseWorldLandmarks`(미터 단위 3D)를 행위 인식에 재활용했습니다.

- **실제로 부딪힌 문제 (foreshortening)**: 정면으로 뻗는 잽은 팔이 카메라 축 방향으로 나가기 때문에 **2D 화면 좌표에서는 오히려 어깨→손목 거리가 짧아 보입니다.** 2D만으로 판정하면 정면 펀치를 놓칩니다.
- **수치로 확인한 문제와 해결**: 2D 어깨폭 단위 거리는 가드 0.86 → 정면 잽 최대 신전 시 **0.35로 오히려 감소**. 반면 3D world 거리는 가드 0.21m → 정면 잽 **0.58m로 정상 증가**. 3D 전환으로 이 오판정을 해결했습니다.
- 이 지점이 "복싱"이 갖는 또 다른 강점입니다 — **정면 찌르기 동작이 있는 스포츠**라서, "왜 2D 포즈만으론 부족하고 3D가 필요한가"를 숫자로 증명하는 데모를 자연스럽게 만들 수 있습니다.

### (3) 그 외 (경량 적용/확장 예정)
- `08. 이미지 복원·화질 개선` — 딥러닝 복원 대신 CSS 필터·엣지 글로우로 저조도/엣지 강조를 경량 구현.
- `03. 픽셀 분할` — SelfieSegmentation으로 플레이어 실루엣을 3D 아레나에 합성하는 **진짜 AR화**를 다음 우선순위로 계획 (`TODO.md`).

---

## 4. 요약 — 한 문장 피치

> **"교수님이 주신 2인용 규칙기반 포즈 복싱 실습을, 4대의 PC를 하나의 실시간 서버로 묶어 4인 동시 대전이 가능한 시스템으로 일반화하고, 그 과정에서 드러난 2D 포즈의 한계(foreshortening, 손가락 추적의 비효율)를 3D world landmark와 GPU 시계열 딥러닝(BiLSTM)으로 정량적으로 해결했다."**

이 한 문장 안에 과제의 네 평가 기준이 모두 들어 있습니다:
- **실용성**: 별도 장비 없이 노트북 웹캠만으로 다자간 체감형 게임 구현
- **비단순성**: 실습6을 단순 재현하지 않고 다자간 실시간 동기화 시스템으로 확장
- **창의성**: "PC 4대 = 4인 인터랙션"이라는, 흔한 1인용 웹캠 게임과 다른 축의 아이디어
- **문제해결(Show Numbers)**: 33.3%→100% 정확도, 42→7 노드 경량화, 2D→3D 전환의 정량적 근거(0.86→0.35 vs 0.21m→0.58m)
